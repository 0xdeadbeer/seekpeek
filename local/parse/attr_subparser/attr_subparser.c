#include "attr_subparser.h"

void attr_subparser_p_tag(GtkWidget *widget, lxb_dom_node_t *node) {
    // lxb_dom_element_t *element = lxb_dom_interface_element(node); 
    // if (element->first_attr == NULL)
    //     return;
    
    // lxb_dom_attr_t current_attr = element->first_attr; 
    // while (current_attr != NULL) {

    //     lxb_char_t attr_value = current_attr->value->data; 
    //     size_t attr_length = current_attr->value->length;

    //     switch (current_attr->upper_name) {
    //         case LXB_DOM_ATTR_COLOR: 
                


    //             gtk_widget_add_css_class(widget, attr_fixed_value); 
    //             break; 
    //         case LXB_DOM_ATTR_SIZE:
    //             break; 
    //     }

    //     current_attr = current_attr->next; 
    // }
}